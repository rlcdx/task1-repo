generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model songs {
  id          Int    @id @default(autoincrement())
  title       String
  releaseYear Int
  artist      String

  @@unique([title, artist])
}
